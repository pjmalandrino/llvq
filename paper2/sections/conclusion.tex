\section{Conclusion}
\label{sec:conclusion}

The GPU can read a Leech-lattice code as it is stored. In \Tetra{}, a block
of weights is read through a 64-state trellis of the Golay code and one
16~KiB table. The bits read per weight drop from 4.804 to 2.148. In exchange,
the codebook bounds each third of a block separately instead of the whole
block.

Three more changes, which leave the lattice codes untouched, give the sealed
files of Qwen3-4B, 8B and 14B at about 2.7 bits per parameter. They score
63.37, 69.58 and 75.66 on MMLU. That is 4.76, 4.21 and 2.46 points below 4-bit
AWQ, and the gap shrinks as the model grows. Our files use about half as much
memory as AWQ. In our engine, on one L40S, they generate 113.8, 95.0 and 57.2
tokens per second, with 1.38, 2.76 and 5.04~GB of weights.

Next, in this order, we would test \texttt{down\_proj} instead of
\texttt{o\_proj} in int4 at 4B and 14B, as at 8B. Then we would add a second
calibration draw per size, a model larger than 14B, and a second kind of GPU.
