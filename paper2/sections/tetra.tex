\section{The Tetra codebook}
\label{sec:tetra}

\subsection{The lattice and the quantizer}

We use the integer form of the Leech lattice, up to the overall scale that the
quantizer sets. A vector of 24 integers $y\in\mathbb{Z}^{24}$ lies in
$\Lambda_{24}$ when it can be written as
\begin{equation}
y_j = p + 2c_j + 4k_j,\qquad \textstyle\sum_j k_j \equiv p \pmod 2 .
\label{eq:leech}
\end{equation}
Here $p$ is a bit, the $k_j$ are integers, and $c$ is a codeword of the binary
Golay code $\mathcal{G}_{24}$. Two constraints pick the lattice out of
$\mathbb{Z}^{24}$ (Figure~\ref{fig:geom}a). First, $c$ must be one of the
$2^{12}$ Golay codewords among the $2^{24}$ bit patterns. Second, one parity
check ties all 24 coordinates together. A decoder has to respect both.

The encoder uses the shape-gain scheme of \citet{llvq2026}. It rotates the
input~\citep{quip2023,quarot2024} and runs a GPTQ-style loop~\citep{gptq2023}.
For each block of 24 rotated weights, it finds the nearest lattice point under
a length cap and rebuilds the weights as
\begin{equation}
\hat{w}_j = \frac{y_j}{\lVert y\rVert}\,\mathrm{gain}[g]\,\sigma_{\mathrm{row}},
\qquad \lVert y\rVert=\sqrt{16m},
\label{eq:recon}
\end{equation}
where $m$ is the shell index. The bit $g$ selects the gain, and
$\sigma_{\mathrm{row}}$ is one f32 scale per output row. We change two things:
the region the encoder searches (\S\ref{sec:kept}) and the bits it writes for
each block. The loop, the rotation, the gain values and the row scales stay as
they were. The encoder still runs once, offline, on a CPU.

\subsection{Three octads and a trellis}

An octad is a Golay codeword with exactly eight ones. Three octads that share
no position cover all 24 coordinates. We fix one such triple and read the
coordinates octad by octad. Cutting after coordinates 8 and 16 splits the
block into three sections of eight coordinates, one per octad. These cuts turn
the code into a trellis (Figure~\ref{fig:geom}b), a layered graph whose paths
spell out bit patterns. The node a path crosses at a cut is its state. Each cut
has 64 states, and no three-way cut of $\mathcal{G}_{24}$ has fewer. The
trellis has $64\times2\times16\times2 = 4096 = |\mathcal{G}_{24}|$ paths, so
every codeword is one path and every path is a codeword. We check this count
when we build the tables.

\begin{figure}[t]
\centering
% (a) the two constraints of Equation (1)
\begin{tikzpicture}[x=0.42cm, y=0.52cm]
\node[font=\scriptsize, anchor=east] at (-0.4,3.4) {$y_j\;=$};
\node[font=\scriptsize, anchor=east] at (-0.4,2.4) {$p\;+$};
\node[font=\scriptsize, anchor=east] at (-0.4,1.4) {$2c_j\;+$};
\node[font=\scriptsize, anchor=east] at (-0.4,0.4) {$4k_j$};
\foreach \i in {0,...,23}{
  \draw[okGray, fill=black!7] (\i,3.15) rectangle ++(0.8,0.5);
  \draw[okGray] (\i,2.15) rectangle ++(0.8,0.5);
  \draw[okGray] (\i,1.15) rectangle ++(0.8,0.5);
  \draw[okGray] (\i,0.15) rectangle ++(0.8,0.5);}
\foreach \i in {0,2,3,4,7,10,12,13,15,17,18,20,23}{
  \draw[okGray, fill=okBlue!35] (\i,1.15) rectangle ++(0.8,0.5);}
\node[font=\scriptsize, anchor=north west] at (0,-0.25)
  {(i)\; $c\in\mathcal{G}_{24}$:\; $2^{12}$ of the $2^{24}$ bit patterns};
\node[font=\scriptsize, anchor=north west] at (0,-1.05)
  {(ii)\; $\textstyle\sum_j k_j \equiv p \pmod 2$:\; one parity over all 24 coordinates};
\end{tikzpicture}\\[6pt]
{\footnotesize (a) $\mathbb{Z}^{24}$ under two global constraints}\\[10pt]
% (b) the same code in octad order
\begin{tikzpicture}[x=1cm, y=1cm]
\node[box, font=\scriptsize] (s0) at (0.5,1.3) {$1$};
\node[mem, font=\scriptsize, minimum width=9mm, minimum height=13mm]
  (s8) at (4.2,1.3) {$64$};
\node[mem, font=\scriptsize, minimum width=9mm, minimum height=13mm]
  (s16) at (8.2,1.3) {$64$};
\node[box, font=\scriptsize] (se) at (11.9,1.3) {$1$};
\draw[flow] (s0) -- node[above, font=\scriptsize]{2 prefixes}
                   node[below, font=\scriptsize]{$b_1$: 1 bit} (s8);
\draw[flow] (s8) -- node[above, font=\scriptsize]{16 branches}
                    node[below, font=\scriptsize]{$b_2$: 4 bits} (s16);
\draw[flow] (s16) -- node[above, font=\scriptsize]{2 suffixes}
                     node[below, font=\scriptsize]{$b_3$: 1 bit} (se);
\node[font=\scriptsize, anchor=north, align=center] at (4.2,0.45)
  {$s_8$: the 6 bits\\the word stores};
\node[font=\scriptsize, anchor=north, align=center] at (8.2,0.45)
  {$s_{16}$: not stored,\\returned with the byte};
\node[font=\scriptsize, anchor=north] at (6.2,-0.55)
  {$64\times2\times16\times2 \;=\; 4096 \;=\; |\mathcal{G}_{24}|$};
\end{tikzpicture}\\[4pt]
{\footnotesize (b) the same code in octad order}
\caption{The lattice and the reordering. (a) Equation~\eqref{eq:leech}, with
the shaded cells marking $c_j=1$. Three separate 8-dimensional quantizers
would respect neither constraint. (b) In octad order the
same code is a trellis, and a codeword is a path through it.}
\label{fig:geom}
\end{figure}

\subsection{State, rank vectors, and one table for three sections}
\label{sec:state}

The 48-bit word of a block (\S\ref{sec:decode}) holds eight bits of state.
They are the trellis state $s_8$ (6 bits), the shared parity $p$, and $r$, the
parity of $\sum k_j$ over section 1. The state $s_8$ holds exactly
what section 2 needs to know about section 1. On top of the code's own state,
the lattice adds only two bits, $p$ and $r$. That gives $64\times4 = 256$
lattice states at a cut.

Each section has a $k$-parity, the parity of the sum of its $k_j$. Only
section 1's is stored, as $r$. Section 2's, $\delta$, is read from its row
index (explained below). Section 3's is
\begin{equation}
r_3 = p \oplus r \oplus \delta ,
\label{eq:xor}
\end{equation}
so the parity constraint of Equation~\eqref{eq:leech} costs one XOR and no bits.

A section's pattern byte is the eight bits of $c$ that fall in it. Once $p$
and this byte are known, each coordinate's remainder mod 4 is fixed,
$o_j = p + 2c_j$. Coordinate $j$ can then only take the values
$o_j + 4\mathbb{Z}$, spaced 4 apart. We store its \emph{rank}, its position in
that list counted outward from zero, instead of its value. For $o=0$ the
values are $0, +4, -4, +8, \dots$, for $o=2$ they are $+2, -2, +6, \dots$, for
$o=1$ $+1, -3, +5, \dots$, and for $o=3$ $-1, +3, -5, \dots$. A section is
then a rank vector $\rho \in \{0,\dots,7\}^8$, four bits per rank, which fits
in one 32-bit table row.

Two properties let a single table serve every section of every block. First,
let $\mathrm{cost}(\rho)=\sum_j(2\rho_j+1)^2$, a stand-in for squared
distance. Sorting rank vectors by this cost gives the same order for every $p$
and every pattern byte. ``Keep the 2{,}048 cheapest'' then means one list
instead of $4^8$. Second, $\mathrm{cls}(\rho)=\sum_j[\rho_j\in\{1,2\}] \bmod 2$ equals
the parity of $\sum_j k_j$ for every pattern. We sort the table by this
class, so a row's position gives its parity with no extra field.

The table holds 4{,}096 rows: the 2{,}048 cheapest rank vectors of class 0,
then the 2{,}048 cheapest of class 1. Eleven bits pick a row within one half.
The middle section reads the same rows through its own list, the 2{,}048
cheapest overall, whatever their class. That list is $N_0 = 1240$ rows of
class 0, then 808 of class 1. The middle index alone thus gives the class of
its row, the $\delta$ that Equation~\eqref{eq:xor} needs.

\subsection{The word and the decode}
\label{sec:decode}

Figure~\ref{fig:word} shows the resulting 48-bit word next to the \Planes{}
record it replaces. Each field can take a power-of-two number of values, so
any 48-bit value is a legal word and decodes to a point of $\Lambda_{24}$. A
benchmark can therefore stream random words and check every decode against a
reference. Figure~\ref{fig:decode} shows the decode: six loads per block,
three of them in a chain where each load needs the one before.

\begin{figure}[t]
\centering
\includegraphics{fig_word.pdf}
\caption{The record of our earlier layout and the \Tetra{} word, drawn to the
same scale in bits, with the word magnified below to label its ten fields.
\Planes{} is what the 47-bit index becomes after unfolding: a 9-bit class, a
gain bit, a sign mask and three bit planes. That is 112 bits per record, one
record every 14 bytes. \Tetra{} is what the file stores and the kernel reads: 48 bits, one
word every 6 bytes. Six loads decode it from 18{,}816~B of tables,
16~KiB of which hold the rank rows. Purple marks trellis state, grey the edge
choices, blue the row indices into the shared table and red the gain bit.
Hatching is padding. Both carry 2.000 bits of code per weight.}
\label{fig:word}
\end{figure}

\begin{figure}[t]
\centering
\begin{tikzpicture}[x=1cm, y=1cm]
\node[font=\footnotesize\bfseries, anchor=south] at (4.8,2.62)
  {one block: six loads, three of them dependent};
\node[box, font=\scriptsize, text width=2.7cm] (w) at (4.8,2.1)
  {the 48-bit word};
\node[mem, font=\scriptsize, text width=4.2cm, align=left] (L) at (2.4,0.95)
  {\textbf{trellis chain}, 2\,304~B\\[1pt]
   \texttt{prefixes}$[2s_8{+}b_1]$\\
   \texttt{branches}$[16s_8{+}b_2]$\\
   \texttt{suffixes}$[2s_{16}{+}b_3]$\\[1pt]
   \emph{chained: each read needs\\the previous one}};
\node[mem, font=\scriptsize, text width=4.2cm, align=left] (R) at (7.2,0.95)
  {\textbf{rank table}, 16~KiB\\[1pt]
   \texttt{rows}$[2048r{+}i_1]$\\
   \texttt{rows}$[\mathrm{mix}(i_2)]$\\
   \texttt{rows}$[2048r_3{+}i_3]$\\[1pt]
   \emph{independent: issued together}};
\node[box, font=\scriptsize, text width=7.2cm] (y) at (4.8,-0.85)
  {24 values $y_j$, then $\times\,\mathrm{gain}[g] \times 1/\!\sqrt{16m}$};
\draw[flow] (w.south west) -- (L.north);
\draw[flow] (w.south east) -- (R.north);
\draw[flow] (L.south) -- (y.north west);
\draw[flow] (R.south) -- (y.north east);
\end{tikzpicture}
\caption{The decode. The three trellis lookups each return one Golay pattern
byte. They must run in order, because each index depends on the state the
previous read returned. The three row reads need only the word, so the kernel
issues them together. The shell index $m$ is recomputed from the decoded
values, not stored. The parity $r_3 = p \oplus r \oplus \delta$ comes from
Equation~\eqref{eq:xor}.}
\label{fig:decode}
\end{figure}

The tables take 18{,}816~B in total. The rank table holds 4{,}096 rows, or
16{,}384~B. The branch table holds 1{,}024 16-bit entries, or 2{,}048~B. The
prefix and suffix tables take 128~B each. The last table holds 32 floats, the
values $1/\sqrt{16m}$. Its entry 0 is 0, so the origin, a legal word, decodes
to zeros with no special case. The bound $m\le27$ comes from trying all
4{,}096 rows with both parities and both residues, which also gives
$\max|y_j|=10$.

\subsection{What the construction costs}
\label{sec:kept}

\Tetra{} is a new codebook on the same lattice, with a different set of points
from the earlier one. Keeping the 2{,}048 cheapest rank vectors limits each
section on its own. The encoder therefore searches a product of three
8-dimensional regions, one per section, where \Planes{} searched one
24-dimensional ball. This split makes the six-load decode possible, and it is
the price the format pays.

We estimate this price instead of measuring it. The shaping gain measures how
much the shape of the searched region lowers the error, compared with a cube.
Its best value for a product of three 8-dimensional balls is 0.7292~dB,
against 1.0958~dB for one 24-dimensional ball. The difference, 0.3666~dB, means 8.81\,\% more mean
squared error for the three balls at our operating point. Our regions are
cut by rank and by the trellis, so they are neither balls nor continuous.
These figures are a geometric reference, not the cost of this codebook. We
measured one related effect. Sorting rank vectors by $\mathrm{cost}(\rho)$
instead of by exact squared distance loses 0.6 points of retention against an
exact enumeration of each section. Retention is the bit rate the theoretical
limit would need to reach our error, as a percentage of the bit rate we use.

The construction does keep the lattice. Every decoded point meets both
constraints of Figure~\ref{fig:geom}. Its pattern bytes form a Golay codeword
because they come from a trellis path, and Equation~\eqref{eq:xor} gives the
$\sum k$ parity. Three separate $E_8$ quantizers ($E_8$ is the best lattice
in 8 dimensions) would keep neither. The
lattice alone is worth $G(E_8)/G(\Lambda_{24}) = 0.071682/0.065771 =
1.0899$~\citep{conway1999sphere}. Here $G$ is the normalized second moment,
the mean squared error of a lattice used as a quantizer at a fixed density of
points. By this measure, $E_8$ has 9.0\,\% more mean squared error than
$\Lambda_{24}$.

On the same model and evaluation code, \Tetra{} alone (without the changes of
\S\ref{sec:model}) scores 2.10 MMLU points below \Planes{} on a
2{,}280-question sample of MMLU. Its perplexity is 4.64\,\% lower, which is better.
Re-encoding one model with three different draws of calibration text moves
MMLU by 2.92 points. That is more than the gap between the two codebooks, so
this comparison does not separate the codebook from the calibration draw.
