\section{The ten-arm kernel benchmark}
\label{app:bench}

\begin{table}[H]
\centering\small\setlength{\tabcolsep}{5pt}
\begin{tabular}{lrrrrr}
\toprule
arm & matrices & median ms & GB read & \bw{} & GB/s\\
\midrule
FP16, our control & 252 & 10.973 & 7.27 & 16.000 & 662\\
FP16, cuBLAS & 252 & 10.828 & 7.27 & 16.000 & 672\\
\textsc{Slot32} (ours, earlier) & 252 & 5.732 & 2.50 & 5.510 & 437\\
\Planes{} (ours, earlier) & 252 & 4.997 & 2.18 & 4.804 & 437\\
\textsc{Planes12x} (ours, earlier) & 252 & 5.366 & 1.97 & 4.342 & 368\\
\textsc{Golay70} v1 (ours, earlier) & 252 & 8.064 & 1.63 & 3.589 & 202\\
\textsc{Golay70} v2 (ours, earlier) & 252 & 6.016 & 1.63 & 3.589 & 271\\
\Tetra{} (this paper) & 216 & 3.424 & 0.95 & 2.148 & 278\\
AWQ w4g128~\citep{awq2024} & 252 & 3.261 & 1.90 & 4.179 & 583\\
no-weights control & 252 & 2.289 & 0.07 & 0.159 & 32\\
QTIP 2-bit~\citep{qtip2024}$^{*}$ & 252 & 2.246 & 0.91 & 2.000 & 405\\
\bottomrule
\end{tabular}
\caption{Each row is one kernel timed on the 4B matrix shapes on one L40S,
over seven rounds with the first two dropped. The first ten rows are the ten
arms of one process, at tile 64: our kernels copy the activation to shared
memory 64 activation blocks at a time. The no-weights control has the same thread
layout and reads no weight byte. The \Tetra{} arm covers 216 matrices and the
others 252, so the milliseconds do not rank the arms. $^{*}$This row comes from
another process (2026-08-21) and is below the no-weights control. We keep it
with that caveat. Data: echelle-formats.csv.}
\end{table}

\section{Record of predictions, defects and commands}
\label{app:record}

\paragraph{Predictions and how they scored.} Before each campaign ran, we
preregistered a prediction and an interval. A signed value with no unit is in
MMLU points.

\begin{table}[H]
\centering\small\setlength{\tabcolsep}{5pt}
\begin{tabular}{lrrl}
\toprule
quantity & predicted & measured & inside?\\
\midrule
\Tetra{} time at tile 64 & 3.77~ms $[3.5, 4.1]$ & 3.423~ms & no, faster\\
gain from tile 128 to 64 & $+8.3\,\%$ $[0, +15]$ & $+16.1\,\%$ & no, larger\\
row-scale training, 4B & $+3.5$ $[+1.5, +4.5]$ & $+3.15$ & yes\\
row-scale training, 8B & $+2.0$ $[+0.5, +3.5]$ & $+3.29$ & yes\\
row-scale training, 14B & $+3.2$ $[+1.7, +4.7]$ & $+1.67$ & no, 0.03 under\\
sealed composition, 4B & $+1.2$ $[+0.2, +2.2]$ & $+2.26$ & no, 0.06 over\\
sealed composition, 14B & $+1.0$ $[0, +2.0]$ & $+1.46$ & yes\\
8B at 2.7, $o$ and down minus down only & $+0.2$ $[-0.6, +1.0]$ & $-0.77$ & no, other sign\\
second training, 4B & $+0.8$ $[-0.4, +2.0]$ & $+0.02$ & yes\\
AWQ 4B, full test set & 69.9 $[69.2, 70.6]$ & 68.14 & no, lower\\
IQ2\_XXS 4B, full test set & 39.0 $[37.5, 40.5]$ & 39.78 & yes\\
AWQ tok/s in vLLM, 8B & 90 $[75, 110]$ & 123.2 & no, faster\\
AWQ tok/s in vLLM, 14B & 55 $[45, 70]$ & 77.8 & no, faster\\
sealed 4B tok/s, GB & 110 $[95, 125]$, 1.38 & 113.8, 1.38 & yes\\
sealed 8B tok/s, GB & 100 $[88, 112]$, 2.77 & 95.0, 2.76 & yes\\
sealed 14B tok/s, GB & 58 $[50, 66]$, 5.04 & 57.2, 5.04 & yes\\
256 identical tokens & at each size & 4B, 8B; 14B to 78 & no at 14B\\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Defects found by the served runs.} The first served runs of the
three sealed files failed on two defects in our engine, before producing any
number. First, projections that read the same input form a group. A group with
no rotation took a code path that sends all prompt rows to the matrix-vector
product at once. That is correct for a dense matrix and wrong for the int4
kernel, which takes one row. A lone int4 \texttt{o\_proj} or
\texttt{down\_proj} forms such a group. An int4 \texttt{v\_proj} never does,
since $q$ and $k$ share its group. Second, the int4 kernel was capped at the
default 48~KiB of shared memory. A 14B \texttt{down\_proj} needs more. We
fixed, tested and type-checked both before the runs behind the speeds in
Table~\ref{tab:main}. Neither defect affects an MMLU number, because MMLU is
scored on the dense reconstruction, which does not run the kernel.

\paragraph{A corrected IQ2\_XXS rate.} An earlier version of our 4B table gave
IQ2\_XXS 2.4967 bits per parameter. That is the rate of a different IQ2\_XXS
file, published by a third party and never scored here. The file we score is
our own build: 1{,}246{,}620{,}832 bytes, or 2.479 bits per parameter.

\paragraph{The files and the commands.} The three sealed files, with the
first eight hex digits of their SHA-256:
\begin{itemize}
\item \nolinkurl{qwen3-4b-sealed.bin}, \texttt{886391a8};
\item \nolinkurl{qwen3-8b-sealed-B.bin}, \texttt{7bdb9a55};
\item \nolinkurl{qwen3-14b-sealed.bin}, \texttt{61db37fe}.
\end{itemize}
Each file has a served configuration in \nolinkurl{configs/}, named after its
size, for example \nolinkurl{qwen3-4b-tetra-e4.json}. On an NVIDIA GPU:

{\footnotesize
\begin{verbatim}
# tok/s and GB, through the served kernel
LLVQ_CONFIG=configs/qwen3-4b-tetra-e4.json cargo run --release \
  -p llvq-llm --features cuda --bin fusedrun -- <file>
# MMLU on the full test set, then a paired comparison of two dumps
LLVQ_MMLU_ALLOC=flat cargo run --release -p llvq-llm \
  --features cuda --bin mmlu -- <file> cuda
cargo run --release -p llvq-llm --bin mmlupair -- <A> <B> --no-fpc
# bits per parameter over the whole model
cargo run --release -p llvq-bench --bin rtbits -- <file>
\end{verbatim}}

\noindent Each program prints a fingerprint of the questions or tokens it
scored. Two numbers compare only when their fingerprints match.

\section{Provenance}
\label{app:provenance}

Each number is \emph{measured} (read off a run), \emph{computed} (arithmetic
on measured quantities) or \emph{cited}. The logs are files under
\texttt{docs/mesures/}. The table shortens their names, and all dates are in
2026. The script \texttt{scripts/make\_figures.py} draws every figure from a
CSV in \texttt{docs/data/}. A second script, \texttt{scripts/check\_tables.py},
stops the build if a table no longer matches its CSV.

{\footnotesize\setlength{\tabcolsep}{4pt}
\begin{tabular}{p{4.8cm}lp{5.2cm}}
\toprule
claim & nature & source\\
\midrule
\Planes{} reads 4.804 \bw{} & measured & echelle-formats.csv; e2-golay70-bench-08-07\\
ball holds $1.1\cdot10^{14}$ points & computed & theta-series test in \texttt{llvq-core}\\
64 states a cut, 1{,}024 edges, $N_0=1240$ & computed & f1a-comptes-09-04; checked at build\\
tables, 18{,}816~B & measured & printed at every launch; banc-t64-09-20\\
$m\le27$, $\max|y_j|=10$ & computed & \texttt{examples/tetrashell.rs}, exhaustive\\
about 141 instructions a block & computed & \texttt{llvq\_f1rank\_v3.cuh}\\
shaping, 0.7292 against 1.0958~dB & computed, idealised & \texttt{ops/f1a\_shaping.py}\\
$G(E_8)/G(\Lambda_{24})=1.0899$ & cited & Conway and Sloane, Table 2.3\\
ten-arm benchmark, Table~\ref{tab:bench} & measured & echelle-formats.csv; banc-t64-09-20\\
tile sweep & measured & tuile-l40s.csv; tuile-l40s-09-20\\
sealed files: bytes, bits per parameter & measured, computed & \texttt{rtbits}; embed-q4-swap-09-23, sealed-8b-14b-09-23, sealed-8b-27-09-24\\
MMLU of the sealed files & measured & same logs; question fingerprint \texttt{a74a6d62}\\
steps of Table~\ref{tab:chain} & measured & paper2-chain.csv; dclm-rowscales-09-20, dclm-8b-rowscales-09-21, dclm-14b-rowscales-09-22, census-8b-09-21, census-14b-base-09-22\\
FP16 and AWQ MMLU, 8B and 14B & measured & census-8b-09-21; census-14b-ref-09-22\\
FP16 MMLU 4B & measured & f16-full-09-18\\
AWQ and IQ2\_XXS MMLU 4B & measured & paper-table-09-25\\
paired gaps, Figure~\ref{fig:scale} & measured & paper2-gaps.csv; the same logs\\
FP16 and AWQ tok/s in vLLM & measured & awq-vllm-4b-08-17; paper-table-09-25\\
IQ2\_XXS tok/s & measured & m4-iq2-cuda-08-30\\
FP16 tok/s, our engine & measured & paper-table-09-25, dense arm of each served run\\
served tok/s and GB of the sealed files & measured & paper-table-09-25\\
8B choice, $o$ against down & measured & sealed-8b-27-09-24\\
second training, 4B & measured & sealed-rownorms-09-23\\
calibration draw moves MMLU by 2.92 & measured, 3 draws & bruit-mmlu-graines-4b-08-25\\
bare \Tetra{} against \Planes{} & measured & tetra-4b-09-06\\
\bottomrule
\end{tabular}}
