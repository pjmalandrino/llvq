\section{Limitations}
\label{sec:limitations}

\begin{itemize}
\item \textbf{One card.} Every measurement is on one NVIDIA L40S. On an A100,
  with the same protocol, none of our earlier lattice kernels ran faster than
  FP16. The best tile also depends on the GPU (\S\ref{sec:tile}).
\item \textbf{One calibration draw per size.} Encoding the 4B model with three
  draws moved MMLU by 2.92 points, more than some of the gains we report. The
  gains in Table~\ref{tab:chain} compare fixed files on the same questions, so
  this spread does not affect them. A different draw would start from a
  different file.
\item \textbf{Quality is scored on the dense reconstruction}, not through the
  kernel. Per-row checks against f64 and the 256-token comparison tie the two
  together. The tokens are identical at 4B and 8B, and first differ at token 78
  at 14B. We did not also score MMLU through the kernel.
\item \textbf{No perplexity} is reported for the sealed files.
\item \textbf{Batch 1 and short context only.} Nothing here measures several
  requests served together, or long prompts.
\item \textbf{The MMLU of the 2-bit methods QTIP, QuIP\# and the original LLVQ
  is cited.} It comes from other evaluation code and another protocol. Their
  bit rates count only the projection matrices.
\item \textbf{The kernel benchmark does not time equal work.} It times 216
  matrices for \Tetra{} and 252 for every other arm (Table~\ref{tab:bench}).
\item \textbf{The cost of splitting the search region is estimated} on
  idealized regions (\S\ref{sec:kept}). We did not measure it on the codebook
  we use.
\item \textbf{One int4 layer range per size.} At 8B, \texttt{down\_proj} alone
  scored better than \texttt{o\_proj} plus \texttt{down\_proj}, with fewer
  bytes. We did not test this at 4B and 14B.
\end{itemize}
