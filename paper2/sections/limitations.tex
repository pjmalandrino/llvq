\section{Limitations}
\label{sec:limitations}

\begin{itemize}
\item \textbf{One card.} Every measurement is on one NVIDIA L40S. On an A100,
  under the same protocol, none of our earlier lattice kernels beat FP16, and
  the best tile changes with the architecture (\S\ref{sec:tile}).
\item \textbf{One calibration draw per size.} Re-encoding the 4B model with
  three draws moved MMLU by 2.92 points, which is larger than some of the gains
  we report. The gains of Table~\ref{tab:chain} are paired on fixed files, so
  they are not affected; a different draw would start from a different file.
\item \textbf{Quality is scored on the dense reconstruction}, not through the
  kernel. The two are tied by per-row checks against f64 and by the 256-token
  comparison, not by a second MMLU run.
\item \textbf{No perplexity} is reported for the sealed files.
\item \textbf{Batch 1 and short context only.} Nothing here measures serving
  several requests at once or long prompts.
\item \textbf{The other 2-bit methods are cited}, from another harness and
  another MMLU protocol, and their rates count the projections only.
\item \textbf{The kernel benchmark compares unequal work}: 216 matrices for
  \Tetra{} against 252 for the other arms (Table~\ref{tab:bench}).
\item \textbf{The cost of the truncation is estimated}, from idealised regions
  (\S\ref{sec:kept}), and not measured on the codebook we use.
\item \textbf{The choice of int4 projections is one window per size.} At 8B,
  \texttt{down\_proj} beat \texttt{o\_proj} for the same bytes; at 4B and 14B
  that alternative is not tested.
\end{itemize}
