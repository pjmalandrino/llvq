THE SENSITIVITY MAP, PROBED AND VALIDATED ON DIFFERENT TEXT (2026-09-15)
=========================================================================
Code: errmap at d490258. Cost: $0, 2 h 02 of Mac, 13:23:07Z to 15:25:27Z.
Data: docs/data/errmap-heldout-0.6b-2026-09-15.csv. Raw log in the -brut dir.
Supersedes errmap-0.6b-2026-09-15.txt, whose probes read the evaluation set.

WHAT CHANGED SINCE THE MORNING RUN
-----------------------------------
That run probed on wikitext-2 test and validated on wikitext-2 test. Its
transfer to a held-out split measured -0.008: the map reproduced its own
evaluation set and predicted nothing beyond it.

Here the probes read 8 windows of wikitext-2 TRAIN, disjoint from the 64
calibration windows, and both validation splits are wikitext-2 test. Nothing the
map was fitted on is scored.

THE HEADLINE
-------------
Transfer to the held-out split: mean +0.9149, median +0.8982, min +0.6455,
max +1.1961. Ninety-one percent of the predicted improvement appears on text the
map never read.

Sign disagreements: 0 of 30 scorable points. The model never mistakes the
direction of a move.

THE DOMAIN OF VALIDITY, WHICH IS THE POINT
-------------------------------------------
Prediction error as a fraction of the predicted move, split B (held out):

  matrices moved     alpha = 0.5     alpha = 1.0
  1                    +0.103          +0.102
  2                    +0.124          +0.150
  4                    +0.073          +0.105
  8                    +0.062          +0.126
  16                   +0.084          +0.152
  32                   +0.129          +0.226
  64                   +0.166          +0.297
  140                  +0.188          +0.355

The structure is exactly what a diagonal model predicts of itself: the error
grows with the number of matrices moved and with the size of the move, because
both grow the cross terms the surrogate does not carry. It is also always the
same sign — the surrogate OVERSTATES the gain, never understates it, so its
ranking is conservative rather than optimistic.

Read as a rule: **the model predicts within 15 % up to 16 matrices at full
amplitude, and within 20 % up to 140 matrices at half amplitude. It degrades to
35 % at 140 matrices at full amplitude.** That is the domain, measured rather
than assumed.

Random selections behave like ranked ones at the same size, so the accuracy is a
property of the size of the move and not of the map's own ordering.

WHAT THE MOVES ARE WORTH
-------------------------
Measured on split A, 12 windows of wikitext-2 test, never probed:

  moved            NLL                 perplexity            change
  8 matrices    3.73499 -> 3.66946    41.8875 -> 39.2308     -6.34 %
  32 matrices   3.73499 -> 3.59782    41.8875 -> 36.5187    -12.82 %
  140 at half   3.73499 -> 3.60680    41.8875 -> 36.8478    -12.03 %
  140 matrices  3.73499 -> 3.53759    41.8875 -> 34.3838    -17.91 %

At identical rate, format and decoder. The correction is one scalar per matrix
applied to the fitted gain centroids, and the centroids are already stored per
matrix in the artifact, so this costs no bit and changes no table and no kernel.

THE SHAPE OF THE SURFACE
-------------------------
41 of 196 directions concave or flat, against 13 on the morning run. 44
directions invisible to the loss, and they are all q_proj and k_proj, which is
Qwen3's per-head RMS norm making those matrices scale invariant.

The ten costliest directions are dominated by v_proj/o_proj pairs and by the
mlp triple of layer 27. Paired projections agree to four digits, which is what
being in series requires.

Pooled single scale from the map: 0.99040, against the sweep's re-encoded
minimum of 1.02. The two still differ in sign, and the reason is unchanged:
post-hoc scaling and re-encoding are different operations.

LIMITS
-------
One model, one seed. Probes and validation are all wikitext-2: the map
generalizes across SPLITS of one corpus, which is not the same as generalizing
across domains. A C4 validation is the next thing owed, and it is cheap.

The claimed gain over all 196 matrices at their own optimum is -0.3385 of NLL.
The measured one at 140 matrices is -0.1974. The gap is the cross terms, and the
table above is what quantifies it.

147 of 196 optima sit at the edge of the trust region, so the map is bounded by
its own guard rail. A wider region would claim more and predict worse; where
that trade sits is unmeasured.

Post-hoc scaling is not re-encoding. Serving this correction means writing the
scaled centroids into the artifact after the loop, which is exactly what was
measured here — not re-running the loop with scaled centroids, which was
measured at 1.02 and is a different thing.
